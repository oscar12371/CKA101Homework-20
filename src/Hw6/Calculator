package Hw6;

public class Calculator {

	public int powerXY(int x, int y) throws CalException {
		
		if(x == 0 && y == 0) {
			throw new CalException("沒有意義");
		}
		
		if(y < 0 ) {
			throw new CalException("次方不能是負數");
			
		}
		
		
		return (int)Math.pow(x, y);
		
		
		
	}


	
	
}
